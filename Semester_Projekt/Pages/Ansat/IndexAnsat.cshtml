@page
@model Semester_Projekt.Pages.Ansat.IndexModel
@{
}
<form method="post">
    <table class="table"´>
        <thead>
        <tr>
            <th>AnsatID</th>
            <th>Navn</th>
            <th>Telefon</th>
            <th>Type</th>
            <th>Kompetencer</th>

            <th></th>
        </tr>
        </thead>
        <tbody>
        @if(Model.AnsatIndexViewModel.Any())
        {
            @foreach (var ansat in Model.AnsatIndexViewModel)
            {
                <tr>
                    <td>@ansat.AnsatID</td>
                    <td>@ansat.AnsatName</td>
                    <td>@ansat.AnsatTelefon</td>
                    <td>@ansat.AnsatType</td>
                    
                    <td>
                        <a asp-page="./EditAnsat" asp-route-ansatId="@ansat.AnsatID">Edit</a>
                    </td>
                </tr>
            }
        }
        
        </tbody>
        @*<thead>
        <tr>
            <th>Kompetencer</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        @if (Model.AnsatKompetencer.Any())
        {
            @foreach (var k in Model.AnsatKompetencer)
            {
                <tr>
                    <td>@k.KompetenceEntities</td>
                </tr>
            }
        }
        </tbody>*@
    </table>
</form>